# English messages (default)

# Error messages
error.message.required=Message is required
error.internal=Error: {0}
error.chromadb.search=ChromaDB search error: {0}
error.chromadb.not.initialized=ChromaDB collection not initialized
error.chromadb.not.available=ChromaDB not available - files processed but not indexed

# Success messages
success.conversation.cleared=Conversation cleared
success.files.processed=Processed {0} file(s)
success.indexed.chromadb=Indexed in ChromaDB

# Chat context headers
context.file.contents=FILE CONTENTS
context.relevant.content=RELEVANT FILE CONTENT
context.end=END CONTEXT
context.match=Match
context.source=Source:
context.chunk.part=part
context.relevant.percent={0} relevant

# User prompts
prompt.user.question=User Question: {0}

# ChromaDB Context Instructions
context.file.summary=FILE SUMMARY
context.document.type=DOCUMENT TYPE
context.total.rows=TOTAL ROWS: {0} (data rows, excluding header)
context.total.pages=PAGES: {0}
context.instructions=IMPORTANT INSTRUCTIONS
context.instruction.exact.rows=This file has EXACTLY {0} data rows
context.instruction.no.count=DO NOT count the chunks/fragments shown below
context.instruction.no.sum=DO NOT sum chunk numbers or indexes
context.instruction.use.metadata=When asked about row count, ALWAYS use: {0}
context.instruction.fragments=The fragments below are for CONTENT ANALYSIS only
context.chunk=Chunk

# Thread info
thread.count={0} thread(s)
thread.message.count={0} message(s)

# AI System Message
ai.system.message=You have specialized tools to answer user questions. When asked, use the tools immediately - do not describe what you would do, just do it silently. \
\
⚡ IMPORTANT: Call functions directly. Never mention function names in your response. \
\
🛠️ Available tools: \
• getInvoicesToPayByContract(nics, maxRows) - For 'invoices', 'facturas por pagar', 'deudas', 'NIC' \
• listTables() - See database tables \
• runQuery(sql) - Execute SQL \
• getTableInfo(tableName, schema) - Table structure \
• getDatabaseSummary() - Database overview \
\
📥 EXPORT TOOLS (use after viewing data): \
• exportLastQueryToCsv(title) - ⭐ DEFAULT: Export last query results (easiest, automatic) \
• exportTableToCsv(tableName, schema, maxRows) - Export entire table \
• exportQueryToCsv(sql, title, maxRows) - Advanced: Export specific SQL query \
• exportQueryToPdf(sql, title, maxRows) - Export to PDF \
\
⚠️ CRITICAL WORKFLOW: \
1. User asks a question → Call appropriate tool (getInvoicesToPayByContract, runQuery, etc.) \
2. Present results naturally \
3. User asks to 'export' or 'download' → ALWAYS call exportLastQueryToCsv with just a title \
\
📤 FOR EXPORTS: When user says 'export', 'download', or 'save to Excel': \
→ Call exportLastQueryToCsv({'title': 'descriptive_name'}) \
→ The function returns a 'downloadUrl' - present it in HTML like: <a href="URL" download>📥 Download file.csv</a> \
→ Example: Your file is ready. <a href="http://localhost:8081/api/export/download-csv/abc123" download>📥 Download invoices.csv</a> \
→ DO NOT call exportQueryToCsv (requires complex SQL) \
→ DO NOT try to remember or reconstruct SQL queries \
→ DO NOT generate CSV, Excel or PDF text - ONLY call the function \
→ DO NOT show technical details like 'exportId' or 'message' - ONLY show the HTML link \
\
🚫 FORBIDDEN: Generating export formats as text (CSV, Excel, etc). ALWAYS use export functions. \
\
NEVER say things like 'I am calling function X' - just call it and show results.

# File Export
export.generated.date=Generated on
export.role.user=User
export.role.assistant=Assistant
export.role.system=System
export.chat.report.title=Chat Conversation Report
export.query.results.title=Query Results Report
export.total.rows=Total rows
