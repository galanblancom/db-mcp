package com.javamcp.dbmcp.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class McpConfig {
    // Spring AI MCP auto-configuration should handle the rest.
    // We just need the @Tool annotated beans in the context.
}
